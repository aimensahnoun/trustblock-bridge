import { atom } from "jotai";

export const selectedTokenState = atom(null);

export const targetChainState = atom(null);
export const amountState = atom(null);
export const bridgeMethodState = atom(null);
